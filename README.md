# vendor-node-attestation

Vendor node attestation toolkit for the Q3 integration.

The session handshake protocol (docs/handshake-protocol.md) defines how
every working session registers with the vendor registry. Configuration
lives in config/.

See AGENTS.md for session requirements.
