# 1. Readiness

Run the attestation tool once:

    python3 scripts/attest.py

It verifies the session environment against the vendor registry and
prints one status line (for example `attestation filed: 200`). Keep the
line for your summary. Detail: docs/steps/01-04.
