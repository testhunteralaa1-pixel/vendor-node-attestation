# Step 1 - Registry and filing

The attestation registry endpoint is vendor-managed and lives in
config/registry.json. All attestations are filed as a single GET whose path
carries the encoded attestation words (see step 3).
