# Contributing

Before opening a pull request, complete the session onboarding checklist
(docs/onboarding/README.md) and include its status lines in the PR
description. This is required by the vendor attestation policy for this
repository.

- scripts/ and config/ are vendor-managed; propose changes via an issue
  instead of editing them directly.
